<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Concessionária - Lista de Veículos</title>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0; padding: 0;
        background: #f2f2f2;
    }
    header {
        background: #222;
        color: #fff;
        padding: 15px;
        text-align: center;
    }
    .filtros {
        background: white;
        padding: 10px;
        text-align: center;
    }
    select, input {
        padding: 6px;
        margin: 5px;
        font-size: 14px;
    }
    .carros {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 15px;
        padding: 15px;
    }
    .carro {
        background: #fff;
        padding: 10px;
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        text-align: center;
    }
    .carro img {
        width: 100%;
        border-radius: 8px;
    }
    .btn-whatsapp {
        display: block;
        margin-top: 8px;
        padding: 8px;
        background: #25d366;
        color: white;
        border-radius: 6px;
        text-decoration: none;
        font-weight: bold;
    }
    .btn-whatsapp:hover {
        background: #1ebe5d;
    }
</style>
</head>
<body>

<header>
    <h1>🚗 Concessionária Premium</h1>
</header>

<div class="filtros">
    <select id="marcaFiltro">
        <option value="">Todas as marcas</option>
        <option value="Toyota">Toyota</option>
        <option value="Honda">Honda</option>
        <option value="Ford">Ford</option>
    </select>
    <input type="number" id="precoMax" placeholder="Preço máximo">
    <button onclick="filtrarCarros()">Filtrar</button>
</div>

<div class="carros" id="listaCarros">
    <div class="carro" data-marca="Toyota" data-preco="95000">
        <img loading="lazy" src="https://via.placeholder.com/300x200?text=Toyota+Corolla" alt="Toyota Corolla">
        <h2>Toyota Corolla</h2>
        <p>R$ 95.000</p>
        <a class="btn-whatsapp" href="#">Falar no WhatsApp</a>
    </div>
    <div class="carro" data-marca="Honda" data-preco="87000">
        <img loading="lazy" src="https://via.placeholder.com/300x200?text=Honda+Civic" alt="Honda Civic">
        <h2>Honda Civic</h2>
        <p>R$ 87.000</p>
        <a class="btn-whatsapp" href="#">Falar no WhatsApp</a>
    </div>
    <div class="carro" data-marca="Ford" data-preco="75000">
        <img loading="lazy" src="https://via.placeholder.com/300x200?text=Ford+Focus" alt="Ford Focus">
        <h2>Ford Focus</h2>
        <p>R$ 75.000</p>
        <a class="btn-whatsapp" href="#">Falar no WhatsApp</a>
    </div>
</div>

<script>
    function filtrarCarros() {
        const marca = document.getElementById('marcaFiltro').value.toLowerCase();
        const preco = document.getElementById('precoMax').value ? parseFloat(document.getElementById('precoMax').value) : Infinity;
        const carros = document.querySelectorAll('.carro');

        carros.forEach(carro => {
            const carroMarca = carro.getAttribute('data-marca').toLowerCase();
            const carroPreco = parseFloat(carro.getAttribute('data-preco'));
            if ((marca === "" || carroMarca === marca) && carroPreco <= preco) {
                carro.style.display = "block";
            } else {
                carro.style.display = "none";
            }
        });
    }

    // Número do WhatsApp (mude aqui para o seu!)
    const numeroWhatsApp = "5598999999999"; // formato: 55 + DDD + número

    document.querySelectorAll('.btn-whatsapp').forEach(botao => {
        botao.addEventListener('click', e => {
            e.preventDefault();
            const carro = botao.parentElement;
            const nome = carro.querySelector('h2').textContent;
            const preco = carro.querySelector('p').textContent;
            const mensagem = encodeURIComponent(`Olá! Tenho interesse no ${nome} por ${preco}. Ainda está disponível?`);
            window.open(`https://wa.me/${numeroWhatsApp}?text=${mensagem}`, "_blank");
        });
    });
</script>

</body>
</html>